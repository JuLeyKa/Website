[build]
# Falls du ein Build-Script hast, z.B. "npm run build"
# command = "npm run build"
# publish = "dist"

[build.environment]
NODE_VERSION = "18"

[functions]
directory = "netlify/functions" # Pfad zur Function
node_version = "18" # Node.js-Version
